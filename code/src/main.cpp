/**
 * @file main.cpp
 * @brief Entry point for the Global Register Allocator.
 *
 * The main function currently serves as a stub. The interactive
 * menu (Menu::run()) is the primary user interface.
 */

int main(){
    return 0;
}
